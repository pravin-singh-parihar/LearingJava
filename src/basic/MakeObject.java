package basic;

public class MakeObject {
    public static void main(String[] args) {
        MakeObject makeObject=new MakeObject();
        /*
        * Objecct are created for the suppose we have one class like Pravin and we want to call another class like Rushi that's why we created an object Pravin class
        * is the use the functanaliy of the Rushi class
        * -object also crated for the instance variable
        * -object are store in the heap memory
        * using object we call the method and variable */

    }
}
